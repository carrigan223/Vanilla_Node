import React from "react";
import { AppTitle, HeaderContainer } from "./headerStyles.js";

const Header = () => {
  return (
    <HeaderContainer>
      <AppTitle>TaskTracker</AppTitle>
    </HeaderContainer>
  );
};

export default Header;
